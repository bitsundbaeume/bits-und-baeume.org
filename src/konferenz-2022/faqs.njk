---
title: Bits & Bäume - die Bewegung für Digitalisierung und Nachhaltigkeit
description: Die „Bits & Bäume“ wird dezentralisiert fortgeführt, auf dass sie weiter wächst und gedeiht.
layout: layouts/_base.ejs
tags: sitemap

---

<section class="max-w-3xl">
	<h3>Bits & Bäume-Konferenz 2022</h3>
	<h1>Unsere FAQs</h1>

</section>

<section class="accordion max-w-3xl">
	<div>
		{% for item in content.faqs.items %}
		<details class="acc__detail">
			<summary class="acc__summary">
				<span class="acc__question">{{ item.name }}</span>
				<div class="acc__icon"></div>
			</summary>
			<div class="acc__text">{{ item.content | safe }}</div>
		</details>
		{% endfor %}
	</div>
</section>
