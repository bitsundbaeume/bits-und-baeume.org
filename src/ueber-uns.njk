---
title: Bits & Bäume - die Bewegung für Digitalisierung und Nachhaltigkeit
description: Die „Bits & Bäume“ wird dezentralisiert fortgeführt, auf dass sie weiter wächst und gedeiht.
layout: layouts/base.ejs
tags: sitemap
---

<section class="max-w-3xl">
	<h1>Über uns</h1>
	<p>to be done</p>

	<h2>Der Trägerkreis</h2>

	{% for item in traegerinnen.items %}
	<div class="box box-orga">
		<div class="image">
			<img src="{{ item.image }}"
					alt="Logo von {{ item.name }}"
					>
		</div>
		<div class="text">
			<h3>{{ item.name }}</h3>
			<p>{{ item.content }}</p>
		</div>
	</div>
	{% endfor %}

</section>
